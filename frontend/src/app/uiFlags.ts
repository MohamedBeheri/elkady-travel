// Temporary display toggles requested by the business — flip back on when ready.
// Seat numbers are still tracked and allocated normally; this only hides the
// number text from tickets, booking screens, and admin/driver views.
export const SHOW_SEAT_NUMBERS = false
